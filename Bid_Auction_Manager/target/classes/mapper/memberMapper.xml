<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.hakyung.bnamgr.dao.MemberMapper">

<insert id="insertMember" parameterType="member">
	insert into BID_AUCTION_MGR (
		member_id
		, member_pw
		, member_nm
		, member_phone
		, member_email
		, member_gender
		, member_profile
	) values (
		#{member_id}
		, #{member_pw}
		, #{member_nm}
		, #{member_phone}
		, #{member_email}
		, #{member_gender}
		, #{member_profile}
	)
</insert>

<select id="selectMember" parameterType="string" resultType="member">
	select
		member_id
		, member_pw
		, member_nm
		, member_phone
		, member_email
		, member_gender
		, member_profile
		, member_indate
		, member_savedfile
		, member_originalfile
	from
		BID_AUCTION_MGR
	where
		member_id = #{member_id}
</select>

<select id="selectMyProduct" parameterType="string" resultType="product">
	select
		p_num
		, p_kind
		, p_min_price
		, max_party
		, current_party
    	, member_id
    	, p_name
    	, p_indate
    	, p_min_price_op
    	, product_savedfile
    	, product_originalfile
    from
    	bid_auction_mgr_product
    where
    	member_id = #{member_id}
</select>

</mapper>
